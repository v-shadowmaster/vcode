{
    "name": "vcode",
    "version": "1.0.0",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "dev": "bun run --filter '*' dev",
        "dev:cli": "bun run --watch packages/cli/src/index.tsx",
        "dev:server": "bun run --watch packages/server/src/server.ts"
    },
    "dependencies": {
        "geist": "^1.7.2"
    }
}